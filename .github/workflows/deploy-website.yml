name: Deploy website to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "website/**"
      - "Scripts/verify-public-font-license.sh"
      - "Scripts/verify-public-repository-assets.sh"
      - "Tests/Scripts/Fixtures/PublicRepositoryAssets/**"
      - "Tests/Scripts/test-verify-public-repository-assets.sh"
      - "Docs/Website-Asset-Licensing.md"
      - ".github/workflows/deploy-website.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: false

defaults:
  run:
    working-directory: website

jobs:
  build:
    name: Build static site
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Reject prohibited font assets
        run: bash ../Scripts/verify-public-font-license.sh

      - name: Test public repository metadata guard
        run: bash ../Tests/Scripts/test-verify-public-repository-assets.sh

      - name: Audit advertised public refs and release metadata
        env:
          PUBLIC_ASSET_GITHUB_TOKEN: ${{ github.token }}
        run: bash ../Scripts/verify-public-repository-assets.sh "${GITHUB_REPOSITORY}"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: website/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Build static export
        run: npm run build:pages

      - name: Verify exported assets
        run: bash ../Scripts/verify-public-font-license.sh

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: ./website/out

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
