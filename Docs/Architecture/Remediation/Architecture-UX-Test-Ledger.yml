# Canonical durable ledger for Section 14 of the Supra Consolidated Architecture and UX Remediation Plan.
# Every canonical T-/DR- identifier appears exactly once. Pending test paths are deliberate RED-first targets.
schema_version: 1
ledger_id: "supra-architecture-ux-remediation-v1"
canonical_section: "Supra Consolidated Architecture and UX Remediation Plan.md#14"
canonical_plan_revision: 3
canonical_plan_sha256: "5582d4da708d0dfb8ab7ba182da7f6130b35f9b8a10c2d89376ab172c9301bc5"
allowed_statuses: [red, green, green_containment, standing_guard_green, decision_pending]
tests:
  - id: "T-DATA-COURT-01"
    finding_or_invariant: "Explicit alias maps `S.D. Fla.` to the approved district court and not bankruptcy."
    owner_work_package: "WP-1.1"
    owner_phase: "Phase 1"
    target: "SupraResearch Swift package test target"
    implemented_test_path: "Packages/SupraResearch/Tests/SupraResearchTests/ArchitectureUXTDataCourt01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraResearch --filter ArchitectureUXTDataCourt01Tests"
    wire_fixture: "Catalog jurisdiction-courts-v1; semantic identity manifest with 2,990 ordered options; explicit alias S.D. Fla.; approved district option/flsd; real bankruptcy option/flsb forbidden; fuzzy Southern District of Florida and S.D. Fla. 731 Tribunal unresolved; unversioned fixture cannot impersonate v1."
    expected_red_reason: "Observed RED at 3907bee8: JurisdictionCatalog lacked version, resource/identity digests, explicit alias inventory, and exact persisted-identity resolution; strengthened RED at e3052823 caught normalized implicit aliases and at de7a072a pinned the semantic identity manifest; GREEN at 99aeb235."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-DATA-COURT-02"
    finding_or_invariant: "Unknown court shows saved text and Choose Court; no jurisdiction/circuit inference or court-dependent work before resolution."
    owner_work_package: "WP-1.1"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTDataCourt02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTDataCourt02Tests"
    wire_fixture: "Synthetic matter-731 revision 7 with saved court Fictional Maritime Claims Tribunal 731 and no canonical IDs; matter-733 pairs the exact FLSD court with the wrong Fifth Circuit; matter-739 pairs FLSD with its governing Eleventh Circuit; Federal, ca11, flsd, and DEFAULT-000 are forbidden in the unresolved projection."
    expected_red_reason: "Observed RED at fba6a986: canonical matter-identity snapshots and MatterCourtPresentationBuilder were absent; strengthened relationship RED at e550e806 caught an exact court paired with the wrong circuit; GREEN at 4730f92c on the v074 Store substrate from 9b8c19e6."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-DATA-COURT-03"
    finding_or_invariant: "One-time migration review enumerates every unresolved court exactly once and records the user's resolution receipt."
    owner_work_package: "WP-1.1"
    owner_phase: "Phase 1"
    target: "SupraStore Swift package test target"
    implemented_test_path: "Packages/SupraStore/Tests/SupraStoreTests/ArchitectureUXTDataCourt03Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraStore --filter ArchitectureUXTDataCourt03Tests"
    wire_fixture: "File-backed pre-v074 database with unresolved matters matter-731 and matter-733 plus exact-alias matter-739; deterministic queue binds one immutable conversion receipt per unresolved matter; decision-743 resolves matter-731, exact retry is idempotent, conflicting retry is rejected, and receipt/queue state survives reopen."
    expected_red_reason: "Observed RED at b373e643: MattersRepository lacked the unresolved-court queue, atomic resolution, receipt fetch APIs, and their value types; GREEN at 9b8c19e6."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-DATA-PARTY-01"
    finding_or_invariant: "Plaintiff-client fixture produces correct client/designation/opponent/counsel/service-recipient defaults; hardcoded defendant default absent."
    owner_work_package: "WP-1.1"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTDataParty01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTDataParty01Tests"
    wire_fixture: "Synthetic matter-731 revision 7; represented client party-713 is plaintiff; opponent party-719 is defendant; representation-727 supplies Jordan Rowan and service+727@example.test as Counsel for Defendant; hardcoded defendant/client and Counsel for Plaintiff defaults are forbidden."
    expected_red_reason: "Observed RED at fba6a986: structured matter identity and DraftPartyDefaultsBuilder were absent while drafting callers still accepted arbitrary party strings; GREEN at 4730f92c on the v074 Store substrate from 9b8c19e6."
    forbidden_default_or_side_effect: "No hardcoded defendant/client designation, inferred opposing party, cross-matter identity, silent fallback, or mutation without confirmation."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic matter fixture and executed-test manifest at the exact candidate SHA; no installed file artifact is required."
    dependencies: "WP-0.4 canonical ledger; WP-1.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-DATA-PARTY-02"
    finding_or_invariant: "Defendant-client fixture produces the inverse defaults; any manual conflict with canonical data requires confirmation."
    owner_work_package: "WP-1.1"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTDataParty02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTDataParty02Tests"
    wire_fixture: "Synthetic matter-831 revision 7; represented client party-813 is defendant; opponent party-817 is plaintiff; representation-823 supplies Avery Quinn and service+823@example.test as Counsel for Plaintiff; conflicting selection of party-817 requires an exact matter/revision/party/purpose/digest-bound receipt-827."
    expected_red_reason: "Observed RED at fba6a986: canonical party-default projection, typed confirmation requests, and receipt validation were absent; GREEN at 4730f92c on the v074 Store substrate from 9b8c19e6."
    forbidden_default_or_side_effect: "No hardcoded defendant/client designation, inferred opposing party, cross-matter identity, silent fallback, or mutation without confirmation."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic matter fixture and executed-test manifest at the exact candidate SHA; no installed file artifact is required."
    dependencies: "WP-0.4 canonical ledger; WP-1.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-DATA-MIGRATE-01"
    finding_or_invariant: "Migration is idempotent, receipt-producing, and preserves original legacy strings."
    owner_work_package: "WP-1.1"
    owner_phase: "Phase 1"
    target: "SupraStore Swift package test target"
    implemented_test_path: "Packages/SupraStore/Tests/SupraStoreTests/ArchitectureUXTDataMigrate01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraStore --filter ArchitectureUXTDataMigrate01Tests"
    wire_fixture: "Hash-pinned immutable v072/v073 source slices and a rich file-backed v073 graph with exact S.D. Fla. alias, unknown and ambiguous courts, raw legacy strings, related documents/artifacts/exports, zero fabricated parties, one conversion receipt per matter, repeated reopen, and a forced late-v074 collision proving rollback and deterministic recovery."
    expected_red_reason: "Observed RED at b373e643: the registry ended at v073 and exposed no canonical identity schema or conversion receipts; strengthened RED at 54839a67 pinned the coherent resolution-state contract and at 689061b6 pinned exact party/representation columns; GREEN at 9b8c19e6."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-DATA-READY-01"
    finding_or_invariant: "All consumers agree on base readiness across the full fixture table."
    owner_work_package: "WP-1.2"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTDataReady01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTDataReady01Tests"
    wire_fixture: "Synthetic T_DATA_READY_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTDataReady01Tests is pending and the T-DATA-READY-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-DATA-READY-02"
    finding_or_invariant: "Synthetic demo reports exactly three ready documents in every downstream consumer."
    owner_work_package: "WP-1.2"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTDataReady02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTDataReady02Tests"
    wire_fixture: "Synthetic T_DATA_READY_02_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTDataReady02Tests is pending and the T-DATA-READY-02 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-DATA-READY-03"
    finding_or_invariant: "Active model B without vectors reports not-ready everywhere."
    owner_work_package: "WP-1.2"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTDataReady03Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTDataReady03Tests"
    wire_fixture: "Synthetic T_DATA_READY_03_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTDataReady03Tests is pending and the T-DATA-READY-03 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-UX-DELETE-01"
    finding_or_invariant: "Soft delete says Move to Recycle Bin and restores."
    owner_work_package: "WP-1.3"
    owner_phase: "Phase 1"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTUxDelete01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTUxDelete01Tests test"
    wire_fixture: "Synthetic T_UX_DELETE_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTUxDelete01Tests is pending and the T-UX-DELETE-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-UX-DELETE-02"
    finding_or_invariant: "Within deletion/Recycling UI, only verified permanent deletion uses irreversible copy/red semantics."
    owner_work_package: "WP-1.3"
    owner_phase: "Phase 1"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTUxDelete02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTUxDelete02Tests test"
    wire_fixture: "Synthetic T_UX_DELETE_02_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTUxDelete02Tests is pending and the T-UX-DELETE-02 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-UX-SETUP-01"
    finding_or_invariant: "Each typed requirement deep-links/focuses the exact AI Setup row and preserves return state."
    owner_work_package: "WP-1.4"
    owner_phase: "Phase 1"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTUxSetup01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTUxSetup01Tests test"
    wire_fixture: "Synthetic T_UX_SETUP_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTUxSetup01Tests is pending and the T-UX-SETUP-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.4; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-WINDOW-01"
    finding_or_invariant: "Single-window build exposes no second main window and keeps route/matter/controller scope coherent; if multiwindow is approved instead, two-window matter/route/edit/model/failure/relaunch isolation is exact."
    owner_work_package: "WP-1.6"
    owner_phase: "Phase 1"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTWindow01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTWindow01Tests test"
    wire_fixture: "Synthetic T_WINDOW_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: SupraAIApp still uses WindowGroup and UI-test helpers invoke File > New Window, so singleton-window assertions fail."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.6; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-WORK-CONTEXT-01"
    finding_or_invariant: "Each named cross-surface handoff carries exact non-default matter/intent/source-set/authority-packet/work-product/version/return/checkpoint values with no re-entry or implicit fallback."
    owner_work_package: "WP-1.6"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTWorkContext01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTWorkContext01Tests"
    wire_fixture: "Synthetic T_WORK_CONTEXT_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: no compact typed WorkContext carries the non-default source, authority, work-product, return, and checkpoint identities."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.6; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-MUTATION-01"
    finding_or_invariant: "Fault injection across create/edit/delete/restore/pin/reorder/settings/import/export and route-dependent save preserves input and committed state, suppresses success/navigation, and exposes retry/correction; no user path swallows the error."
    owner_work_package: "WP-1.6"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTMutation01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTMutation01Tests"
    wire_fixture: "Synthetic T_MUTATION_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: user mutations still use try? or void callbacks and some views dismiss or navigate without a committed write."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.6; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-REVIEW-RETIRE-UI-01"
    finding_or_invariant: "No Case File Review tab/menu/Go command/setup/status/export/help/accessibility element exists at 880/1100/wide. A targeted route/restoration scan proves whether compatibility decoding exists; absent a real contract, fresh/default selection is Chat and no speculative decoder is added."
    owner_work_package: "WP-1.5"
    owner_phase: "Phase 1"
    target: "SupraAIUITests hosted native-app target"
    implemented_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXRetirementUITests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXRetirementUITests test"
    wire_fixture: "Exact ordered destinations Chat, Research, Authorities, Outputs, Documents, Billing, Audit; fresh Chat selection; widths 880, 1100, and 1420; retired Review button/route/default absent."
    expected_red_reason: "Observed RED at 78b2c633: MatterWorkspaceView still declared and rendered the Review destination and the exact seven-destination invariant failed."
    forbidden_default_or_side_effect: "No live Case File Review capability, admission, runtime reservation, mutation, export, claim, or UI route; immutable compatibility history and unrelated review semantics remain."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-REVIEW-RETIRE-JOB-01"
    finding_or_invariant: "New admission is blocked; queued/paused/running/failed/completed legacy jobs never resume/call model or XPC/reserve runtime/write terminal state; quiescence precedes removal."
    owner_work_package: "WP-1.5"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/RetiredCorpusQueueCompatibilityTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter RetiredCorpusQueueCompatibilityTests"
    wire_fixture: "Synthetic guided-review admission, UUID/run-key and prefixed-ID identities, queued generic follower, paused resume, interrupted active, failed, and completed rows; nondefault run keys 713 through 761."
    expected_red_reason: "Observed RED at a041caaa: admission accepted guided-review work and bootstrap/resume could activate it before the retired-identity policy and exact-ID follower activation existed."
    forbidden_default_or_side_effect: "No live Case File Review capability, admission, runtime reservation, mutation, export, claim, or UI route; immutable compatibility history and unrelated review semantics remain."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-REVIEW-TERMS-01"
    finding_or_invariant: "Research-result review, reviewed authorities, relation review, drafting review notes, citation review, generic needsReview, and Review a Draft remain green and cannot route to Case File Review."
    owner_work_package: "WP-1.5"
    owner_phase: "Phase 1"
    target: "SupraAIUITests hosted native-app target"
    implemented_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXRetirementUITests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXRetirementUITests test"
    wire_fixture: "Exact /critique routing, LegalPromptTemplates critique owner, reviewed proposition state, relation-review transition, citation-review state, generic needsReview, and Review a Draft suggestion."
    expected_red_reason: "Standing guard established at 78b2c633: a broad Review deletion could erase retained attorney-review concepts or reconnect /critique to the retired feature."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "standing_guard_green"
  - id: "T-REVIEW-RETIRE-ARTIFACT-01"
    finding_or_invariant: "Existing Review CSV/XLSX files and model binaries referenced by remaining roles are untouched by retirement/reset/compensation."
    owner_work_package: "WP-1.5"
    owner_phase: "Phase 1"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/RetiredCorpusQueueCompatibilityTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter RetiredCorpusQueueCompatibilityTests"
    wire_fixture: "Existing review-export-713.csv, review-export-719.xlsx, and shared model model-727.safetensors with exact nondefault bytes survive interrupted retired-job bootstrap; DEFAULT-000 absent."
    expected_red_reason: "Standing guard added after removal because retirement has no artifact-cleanup operation: the only legacy-job relaunch reconciliation must not become an export or shared-model filesystem sweep."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-1.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "standing_guard_green"
  - id: "T-PUB-CHAT-01"
    finding_or_invariant: "Fault injection at each aggregate write leaves no terminal partial answer."
    owner_work_package: "WP-2.1"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTPubChat01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTPubChat01Tests"
    wire_fixture: "Synthetic T_PUB_CHAT_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTPubChat01Tests is pending and the T-PUB-CHAT-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-PUB-CHAT-02"
    finding_or_invariant: "Exact retry is idempotent; altered retry rejects without mutation."
    owner_work_package: "WP-2.1"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTPubChat02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTPubChat02Tests"
    wire_fixture: "Synthetic T_PUB_CHAT_02_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTPubChat02Tests is pending and the T-PUB-CHAT-02 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-PUB-WORK-01"
    finding_or_invariant: "Initial and repaired work-product versions publish atomically through the existing repository API."
    owner_work_package: "WP-2.2"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTPubWork01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTPubWork01Tests"
    wire_fixture: "Synthetic T_PUB_WORK_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTPubWork01Tests is pending and the T-PUB-WORK-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-AUTH-01"
    finding_or_invariant: "Authority-asserting type without reviewed packet causes zero model and Store calls."
    owner_work_package: "WP-2.2"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/R0LegalAuthorityContainmentTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter R0LegalAuthorityContainmentTests/testAuthorityAssertingOutputsWithoutReviewedPacketMakeZeroRuntimeAndStoreCalls"
    wire_fixture: "Synthetic matter matter-713; non-default authority types researchPlan, caseResultSummary, ruleSynthesis, and argumentOutline; context MERIDIAN-AUTHORITY-CONTEXT-442; reviewed packet absent; draftingSkeleton default path must not substitute."
    expected_red_reason: "Historical RED: four authority-asserting templates generated and persisted output before downgrading it; the gate now stops before runtime and Store calls."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.2; predecessor RED evidence and exact-head baseline where applicable."
    standing_guard: "GREEN and justified: R0LegalAuthorityContainmentTests/testNonAuthorityScaffoldStillGeneratesAndPersists proves containment does not disable the non-authority drafting scaffold."
    status: "green"
  - id: "T-AUTH-02"
    finding_or_invariant: "Exact authority IDs/excerpts/provenance flow into the published source packet."
    owner_work_package: "WP-2.2"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTAuth02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTAuth02Tests"
    wire_fixture: "Synthetic T_AUTH_02_WIRE_731 matter; provider provider-713; exact query QUERY-CANARY-719; grant version 7; unrelated matter body DEFAULT-BODY-000 must not transmit."
    expected_red_reason: "Expected RED: ArchitectureUXTAuth02Tests is pending and the T-AUTH-02 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-EGRESS-01"
    finding_or_invariant: "Public citation may execute only under the approved classification/policy."
    owner_work_package: "WP-2.3"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/R0LegalAuthorityContainmentTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter R0LegalAuthorityContainmentTests/testPublicCaseCitationUsesOneDeterministicRequestWithoutPlannerOrMatterCanary"
    wire_fixture: "Synthetic matter Synthetic Meridian with client canary MERIDIAN-CLIENT-771; public citation 516 U.S. 349; exactly one CourtListener query equal to that citation; planner/default matter text absent."
    expected_red_reason: "Historical RED: deterministic public-citation lookup invoked the model query planner before provider transport; the non-default matter canary proved the planner path was reachable."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-EGRESS-02"
    finding_or_invariant: "Matter-derived query requires exact displayed/transmitted approval."
    owner_work_package: "WP-2.3"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/R0LegalAuthorityContainmentTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter R0LegalAuthorityContainmentTests/testMatterDerivedQuickResearchStopsBeforeProviderAndDirectsToReviewedResearch"
    wire_fixture: "Synthetic matter Synthetic Meridian with client canary MERIDIAN-CLIENT-771; matter-derived prompt Research Florida successor liability for the Meridian client; zero provider/model/Store calls under R0 containment; implicit auto-send forbidden."
    expected_red_reason: "Historical RED: matter-derived quick research planned and transmitted automatically. R0 is green by stopping before provider/model/Store work; the full positive exact-preview grant path remains a WP-2.3 dependency."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "green_containment"
  - id: "T-EGRESS-03"
    finding_or_invariant: "Provider/origin/query mismatch, replay, and expiry each produce zero transport calls."
    owner_work_package: "WP-2.3"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTEgress03Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTEgress03Tests"
    wire_fixture: "Synthetic T_EGRESS_03_WIRE_731 matter; provider provider-713; exact query QUERY-CANARY-719; grant version 7; unrelated matter body DEFAULT-BODY-000 must not transmit."
    expected_red_reason: "Expected RED: ArchitectureUXTEgress03Tests is pending and the T-EGRESS-03 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-EGRESS-04"
    finding_or_invariant: "Unknown query classification fails closed."
    owner_work_package: "WP-2.3"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTEgress04Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTEgress04Tests"
    wire_fixture: "Synthetic T_EGRESS_04_WIRE_731 matter; provider provider-713; exact query QUERY-CANARY-719; grant version 7; unrelated matter body DEFAULT-BODY-000 must not transmit."
    expected_red_reason: "Expected RED: ArchitectureUXTEgress04Tests is pending and the T-EGRESS-04 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-EGRESS-05"
    finding_or_invariant: "Canary source/quick-attachment body text is absent from outbound queries unless exactly previewed and approved."
    owner_work_package: "WP-2.3"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTEgress05Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTEgress05Tests"
    wire_fixture: "Synthetic T_EGRESS_05_WIRE_731 matter; provider provider-713; exact query QUERY-CANARY-719; grant version 7; unrelated matter body DEFAULT-BODY-000 must not transmit."
    expected_red_reason: "Expected RED: ArchitectureUXTEgress05Tests is pending and the T-EGRESS-05 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-RESEARCH-PACKET-01"
    finding_or_invariant: "Executed/reviewed/accepted transitions and exact grant/provider/query/result/excerpt binding are transactional."
    owner_work_package: "WP-2.6"
    owner_phase: "Phase 2"
    target: "SupraStore Swift package test target"
    pending_test_path: "Packages/SupraStore/Tests/SupraStoreTests/ArchitectureUXTResearchPacket01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraStore --filter ArchitectureUXTResearchPacket01Tests"
    wire_fixture: "Synthetic T_RESEARCH_PACKET_01_WIRE_731 matter; provider provider-713; exact query QUERY-CANARY-719; grant version 7; unrelated matter body DEFAULT-BODY-000 must not transmit."
    expected_red_reason: "Expected RED: ArchitectureUXTResearchPacket01Tests is pending and the T-RESEARCH-PACKET-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.6; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-RESEARCH-PACKET-02"
    finding_or_invariant: "Packet versions are immutable; altered use rejects, exact retry is idempotent, and downstream references stay version-exact."
    owner_work_package: "WP-2.6"
    owner_phase: "Phase 2"
    target: "SupraStore Swift package test target"
    pending_test_path: "Packages/SupraStore/Tests/SupraStoreTests/ArchitectureUXTResearchPacket02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraStore --filter ArchitectureUXTResearchPacket02Tests"
    wire_fixture: "Synthetic T_RESEARCH_PACKET_02_WIRE_731 matter; provider provider-713; exact query QUERY-CANARY-719; grant version 7; unrelated matter body DEFAULT-BODY-000 must not transmit."
    expected_red_reason: "Expected RED: ArchitectureUXTResearchPacket02Tests is pending and the T-RESEARCH-PACKET-02 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.6; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-WORKFLOW-BUDGET-01"
    finding_or_invariant: "Non-default model/tool/token/time/retry/repetition/egress/working-set N/N+1 ceilings stop the exact feature task with no further effects and a typed terminal reason."
    owner_work_package: "WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1"
    owner_phase: "Phases 2-4"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTWorkflowBudget01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTWorkflowBudget01Tests"
    wire_fixture: "Synthetic T_WORKFLOW_BUDGET_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTWorkflowBudget01Tests is pending and the T-WORKFLOW-BUDGET-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-TOOL-AUTH-01"
    finding_or_invariant: "Every tool declares an effect class; denied/unknown/external-write/credential/destructive intentions execute zero effects without the exact applicable grant. Advanced does not broaden authority."
    owner_work_package: "WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1"
    owner_phase: "Phases 2-4"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTToolAuth01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTToolAuth01Tests"
    wire_fixture: "Synthetic T_TOOL_AUTH_01_WIRE_731 matter; provider provider-713; exact query QUERY-CANARY-719; grant version 7; unrelated matter body DEFAULT-BODY-000 must not transmit."
    expected_red_reason: "Expected RED: ArchitectureUXTToolAuth01Tests is pending and the T-TOOL-AUTH-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-OUTCOME-01"
    finding_or_invariant: "A nominally successful mutation with missing/mismatched Store row, file digest, or external postcondition yields failed/unknown and cannot publish terminal success."
    owner_work_package: "WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1"
    owner_phase: "Phases 2-4"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTOutcome01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTOutcome01Tests"
    wire_fixture: "Synthetic T_OUTCOME_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTOutcome01Tests is pending and the T-OUTCOME-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-WORKFLOW-MEMORY-01"
    finding_or_invariant: "Model-generated facts/summaries cannot become durable matter truth or cross matter boundaries without exact source binding or owner-approved state."
    owner_work_package: "WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1"
    owner_phase: "Phases 2-4"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTWorkflowMemory01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTWorkflowMemory01Tests"
    wire_fixture: "Synthetic T_WORKFLOW_MEMORY_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTWorkflowMemory01Tests is pending and the T-WORKFLOW-MEMORY-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-HANDOFF-01"
    finding_or_invariant: "Fixed research/drafting steps exchange the exact typed schema and budget/source identities; raw intermediate model transcripts and unnecessary KV state are absent."
    owner_work_package: "WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1"
    owner_phase: "Phases 2-4"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTHandoff01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTHandoff01Tests"
    wire_fixture: "Synthetic T_HANDOFF_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTHandoff01Tests is pending and the T-HANDOFF-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.3/WP-2.5/WP-2.6/WP-3.1/WP-4.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-ATTACH-01"
    finding_or_invariant: "40,000/40,001 inclusion boundary and per-attachment truncation metadata are exact."
    owner_work_package: "WP-2.4"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTAttach01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTAttach01Tests"
    wire_fixture: "Synthetic T_ATTACH_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTAttach01Tests is pending and the T-ATTACH-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.4; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-ATTACH-02"
    finding_or_invariant: "Quick attachment output remains nondurable/non-promotable even when untruncated."
    owner_work_package: "WP-2.4"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTAttach02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTAttach02Tests"
    wire_fixture: "Synthetic T_ATTACH_02_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTAttach02Tests is pending and the T-ATTACH-02 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-2.4; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-ART-01"
    finding_or_invariant: "Same-second/concurrent exports create identity-owned unique artifacts."
    owner_work_package: "WP-2.5"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTArt01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTArt01Tests"
    wire_fixture: "Synthetic T_ART_01_WIRE_731 artifact; intent artifact-713; digest digest-719; collision suffix 7; default overwrite path DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTArt01Tests is pending and the T-ART-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-2.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-ART-02"
    finding_or_invariant: "Altered or third-party-replaced file is preserved during compensation and marked recovery-required."
    owner_work_package: "WP-2.5"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTArt02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTArt02Tests"
    wire_fixture: "Synthetic T_ART_02_WIRE_731 artifact; intent artifact-713; digest digest-719; collision suffix 7; default overwrite path DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTArt02Tests is pending and the T-ART-02 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-2.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-ART-03"
    finding_or_invariant: "Interrupted intent reconciles correctly on relaunch."
    owner_work_package: "WP-2.5"
    owner_phase: "Phase 2"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTArt03Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTArt03Tests"
    wire_fixture: "Synthetic T_ART_03_WIRE_731 artifact; intent artifact-713; digest digest-719; collision suffix 7; default overwrite path DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTArt03Tests is pending and the T-ART-03 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-2.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-RAG-CORPUS-01"
    finding_or_invariant: "Versioned synthetic legal corpus has exact digests, required OCR/table/revision/contrary/no-answer slices, human-reviewed judgments, and zero real client data."
    owner_work_package: "WP-0.4/WP-3.5"
    owner_phase: "Phases 0 and 3"
    target: "SupraTestKit Swift package test target"
    implemented_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/RAGCorpusManifestContractTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter RAGCorpusManifestContractTests"
    wire_fixture: "Synthetic corpus supra-rag-synthetic-legal v1.0.0; OCR code OCR-F-8842; table value $275,000; amendment/executed/superseded/duplicate evidence; explicit missing settlement release; eight exact artifact digests; approved-review status is forbidden until owner review."
    expected_red_reason: "Historical RED at 375bf480: RAGBenchmarkCorpusManifest and the structural validator were absent, so the selected test failed to compile on the missing type before any fixture could pass."
    forbidden_default_or_side_effect: "No absolute/user/client path, real client data, missing or mismatched digest, undeclared slice, fabricated attorney approval, duplicate evidence identity, accidental empty answerable golden, or nonexact claim/evidence edge."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "TestData/Benchmarks/rag-corpus-v1.json SHA-256 6fa99ce95733c4d0af62035393ea7b5d3635f7cc47e5580547bfdfaee5aca598 plus each source artifact digest; owner judgment approval remains pending."
    dependencies: "WP-0.4 canonical ledger; T-RAG-EVAL-01; owner review of all five query rows before the Phase 0 RAG control can freeze."
    status: "decision_pending"
  - id: "T-RAG-EVAL-01"
    finding_or_invariant: "Hand-calculated Recall@K/full-set, nDCG@K, MRR, context precision/recall, citation precision/coverage, zero-result, tie, duplicate, K-boundary, empty-golden, and N/A fixtures are deterministic."
    owner_work_package: "WP-0.4/WP-3.5"
    owner_phase: "Phases 0 and 3"
    target: "SupraTestKit Swift package test target"
    implemented_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/RAGMetricContractTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter RAGMetricContractTests"
    wire_fixture: "Grades A=3/B=2/C=1 with returned X/B/A at K=3; exact DCG 5.392789, IDCG 9.392789, nDCG 0.574141, recall 2/3, MRR 1/2; B/A tie, duplicate A, empty golden, A/A/X context, exact claim edges, and four zero-result observations."
    expected_red_reason: "Historical RED at a83d360c: ragRankingScore, ragContextScore, ragCitationScore, zeroResultAccuracy, and their result types were absent, producing exact missing-symbol compile errors."
    forbidden_default_or_side_effect: "No unstable tie, duplicate slot consumption, invalid K coercion, silently perfect empty denominator, inferred citation edge, denominator loss, optional advisory override, or regenerated golden."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; RAG metric implementation at 39c54957; frozen pre-existing 28-metric report parity and refreshed fail-closed harness identity at 48ff555c."
    status: "green"
  - id: "T-RAG-BASELINE-01"
    finding_or_invariant: "Control and candidate use the same corpus/config/hardware; one declared variable changes; advisory LLM judge cannot override human/deterministic gates."
    owner_work_package: "WP-0.4/WP-3.5"
    owner_phase: "Phases 0 and 3"
    target: "SupraTestKit Swift package test target"
    implemented_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/RAGControlComparisonContractTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter RAGControlComparisonContractTests"
    wire_fixture: "Exact corpus digest 6fa99ce95733c4d0af62035393ea7b5d3635f7cc47e5580547bfdfaee5aca598; Mac16,7/M4 Pro/48 GiB; source commits c985f954... and 719...; reranker-only candidate; separate reranker+packer drift; advisory evaluator 733; missing OCR citation-recall and N/A combined-peak cells."
    expected_red_reason: "Historical RED at 8fd497b5: RAGBenchmarkRun, RAGRunBinding, RAGControlConfigurationBinding, RAGBenchmarkCell, and the comparison validator were absent and the selected test failed on exact missing-symbol compile errors."
    forbidden_default_or_side_effect: "No corpus/hardware/toolchain mismatch, zero- or multi-variable challenger, advisory override, fabricated measurement, silent N/A, absent critical cell, unbound source SHA, or candidate pass without deterministic authority."
    required_native_evidence: "Selected deterministic package result is green; current-head installed app/XPC control capture and owner-approved noninferiority/resource ceilings remain pending."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; validator implementation at 62387e10; T-RAG-CORPUS-01 owner review; exact installed artifact/prompt inventory; current control run and owner-approved critical-slice/resource thresholds."
    status: "decision_pending"
  - id: "T-EMBED-STREAM-01"
    finding_or_invariant: "Embeddings are generated and committed one bounded batch at a time; peak host vectors stay under cap and crash/relaunch is complete without duplicates."
    owner_work_package: "WP-3.3"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTEmbedStream01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTEmbedStream01Tests"
    wire_fixture: "Synthetic T_EMBED_STREAM_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Observed RED at dab66dc6: semantic indexing accumulated every returned vector until one final commit, and interrupted relaunch recomputed already durable batches. The async assertion authoring defect was corrected separately at 781515c0."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.3; RED at dab66dc6; test correction at 781515c0; bounded durable batch implementation at d924657f."
    status: "green"
  - id: "T-RAG-SCAN-01"
    finding_or_invariant: "SQL-scoped cursor/page plus fixed candidate-K heap matches the reference full scan; stable ties, thresholds, filters, malformed vectors, structure, and neighbor behavior are exact."
    owner_work_package: "WP-3.3"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTRagScan01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTRagScan01Tests"
    wire_fixture: "Synthetic T_RAG_SCAN_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Observed RED at 3d72a76c: semantic retrieval fetched matter/model-wide vectors, materialized and sorted them in memory, and had no bounded page/heap metrics or stable malformed-vector policy. The Float reference fixture was corrected at d8fbe61b."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.3; RED at 3d72a76c; fixture corrections at d8fbe61b/0ad8a788; bounded Store-page scanner at 53874504."
    status: "green"
  - id: "T-RAG-SCAN-02"
    finding_or_invariant: "Large-corpus maximum live page/heap/vector bytes and app+XPC peak stay within non-default ceilings; cancellation releases state and publishes nothing."
    owner_work_package: "WP-3.3"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTRagScan02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTRagScan02Tests"
    wire_fixture: "Synthetic T_RAG_SCAN_02_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Expected RED: no large-corpus bounded live-page/heap/vector and combined app+XPC current/peak proof exists."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-RAG-CACHE-01"
    finding_or_invariant: "Exact per-cache key matrix: every relevant input/artifact/source/config change misses; exact retry hits; readiness and policy are revalidated."
    owner_work_package: "WP-3.3"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTRagCache01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTRagCache01Tests"
    wire_fixture: "Synthetic T_RAG_CACHE_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Observed RED at fe6d8b17: no content-free version-bound semantic candidate cache or complete exact-key matrix existed. Refinements at 866f1838/cd9f88ec proved model artifact identity participates and altered artifacts miss."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.3; RED/refinements at fe6d8b17/866f1838/cd9f88ec; shipping cache implementation at 51c8705a."
    status: "green"
  - id: "T-RAG-CACHE-02"
    finding_or_invariant: "Byte-costed N/N+1 eviction, total budget, memory-pressure purge, corruption/failure fallback, and explicit absence of semantic final-answer caching."
    owner_work_package: "WP-3.3"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTRagCache02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTRagCache02Tests"
    wire_fixture: "Synthetic T_RAG_CACHE_02_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Observed RED at fe6d8b17: no byte-costed RAG LRU, deterministic N/N+1 eviction, pressure purge, corruption fallback, or semantic-answer-cache absence gate existed."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.3; RED at fe6d8b17; bounded candidate-cache implementation at 51c8705a; process-wide weak purge registry at ce32c456."
    status: "green"
  - id: "T-RAG-TRACE-01"
    finding_or_invariant: "Receipt contains required opaque version/rank/timing/resource facts and excludes raw query/source/prompt/answer/credential/file-name canaries."
    owner_work_package: "WP-3.3"
    owner_phase: "Phase 3"
    target: "SupraDiagnostics Swift package test target"
    implemented_test_path: "Packages/SupraDiagnostics/Tests/SupraDiagnosticsTests/ArchitectureUXTRagTrace01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraDiagnostics --filter ArchitectureUXTRagTrace01Tests"
    wire_fixture: "Synthetic T_RAG_TRACE_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Observed RED at 53298a05: no whitelist-encoded content-free RAGExecutionReceipt or deterministic Advanced diagnostics export existed."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.3; RED at 53298a05; content-free receipt/export implementation at a05ac04b."
    status: "green"
  - id: "T-RAG-TRACE-02"
    finding_or_invariant: "Operational receipt cannot authorize egress, establish readiness/provenance/authority, or complete a legal aggregate; Advanced export is redacted."
    owner_work_package: "WP-3.3"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTRagTrace02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTRagTrace02Tests"
    wire_fixture: "Synthetic T_RAG_TRACE_02_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Observed RED at 53298a05: shipping retrieval published no execution receipt and no typed authority boundary prevented operational diagnostics from influencing egress, readiness, provenance, authority, or legal aggregate completion."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.3; RED at 53298a05; shipping retrieval publication and typed authority boundary at a05ac04b."
    status: "green"
  - id: "T-RAG-DEPENDENCY-01"
    finding_or_invariant: "Signed graph contains no unapproved framework, daemon, subprocess, hosted RAG dependency, port/origin, model artifact, or license/SBOM gap."
    owner_work_package: "WP-4.5/WP-6.7"
    owner_phase: "Phases 4 and 6"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXTRagDependency01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXTRagDependency01Tests"
    wire_fixture: "Synthetic T_RAG_DEPENDENCY_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Expected RED: model catalog/manifests lack complete license/SBOM metadata and no signed RAG graph gate exists."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-4.5/WP-6.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "DR-RAG-EXPERIMENT-01"
    finding_or_invariant: "Each challenger records one-variable control, quality/resource/privacy/license/deployment evidence, and exactly adopt/port narrow primitive/no change."
    owner_work_package: "WP-6.7"
    owner_phase: "Phase 6"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXDrRagExperiment01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXDrRagExperiment01Tests"
    wire_fixture: "Synthetic DR_RAG_EXPERIMENT_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Expected RED: ArchitectureUXDrRagExperiment01Tests is pending and the DR-RAG-EXPERIMENT-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-6.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "decision_pending"
  - id: "T-RAG-RERANK-01"
    finding_or_invariant: "Conditional candidate artifact loads in shipping MLX, improves its declared slice without citation regression, respects admission, and unloads."
    owner_work_package: "WP-6.7"
    owner_phase: "Phase 6"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXTRagRerank01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXTRagRerank01Tests"
    wire_fixture: "Synthetic T_RAG_RERANK_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Expected RED: ArchitectureUXTRagRerank01Tests is pending and the T-RAG-RERANK-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-6.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "decision_pending"
  - id: "T-RAG-PARSER-01"
    finding_or_invariant: "Conditional parser improves synthetic extraction/locator/structure goldens; no shipping Python/service dependency enters without separate approval."
    owner_work_package: "WP-6.7"
    owner_phase: "Phase 6"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXTRagParser01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXTRagParser01Tests"
    wire_fixture: "Synthetic T_RAG_PARSER_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Expected RED: ArchitectureUXTRagParser01Tests is pending and the T-RAG-PARSER-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-6.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "decision_pending"
  - id: "T-RAG-CHUNK-01"
    finding_or_invariant: "Conditional versioned chunker improves the declared failure slice without global retrieval/citation regression and has exact reindex/lineage behavior."
    owner_work_package: "WP-6.7"
    owner_phase: "Phase 6"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXTRagChunk01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXTRagChunk01Tests"
    wire_fixture: "Synthetic T_RAG_CHUNK_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Expected RED: ArchitectureUXTRagChunk01Tests is pending and the T-RAG-CHUNK-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-6.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "decision_pending"
  - id: "T-RAG-EMBED-01"
    finding_or_invariant: "Conditional exact embedding artifact loads in MLX; dimensions/fingerprint/readiness/reindex/storage/memory/rollback are exact."
    owner_work_package: "WP-6.7"
    owner_phase: "Phase 6"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXTRagEmbed01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXTRagEmbed01Tests"
    wire_fixture: "Synthetic T_RAG_EMBED_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Expected RED: ArchitectureUXTRagEmbed01Tests is pending and the T-RAG-EMBED-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-6.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "decision_pending"
  - id: "T-RAG-ANN-01"
    finding_or_invariant: "Conditional embedded ANN improves a failed scale target while preserving scope/deletion/revision/citation/recall, corruption rebuild, backup, and rollback."
    owner_work_package: "WP-6.7"
    owner_phase: "Phase 6"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXTRagAnn01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXTRagAnn01Tests"
    wire_fixture: "Synthetic T_RAG_ANN_01_WIRE_731 corpus; query QUERY_713; page 3; K 2; dimension 3; cache ceiling 17 bytes; default K 60 is the forbidden control."
    expected_red_reason: "Expected RED: ArchitectureUXTRagAnn01Tests is pending and the T-RAG-ANN-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No matter-wide materialization, unbounded vector/cache state, unstable tie, stale hit, raw-content receipt, semantic final-answer cache, or undeclared dependency."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-6.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "decision_pending"
  - id: "T-XPC-REVIEW-REMOVE-01"
    finding_or_invariant: "Zero Case File Review lease/reservation/status/wrapper references remain; ordinary model operations, cancellation, status, and crash recovery stay green."
    owner_work_package: "WP-1.5/WP-3.1"
    owner_phase: "Phases 1 and 3"
    target: "SupraAIUITests hosted native-app target plus SupraRuntimeClient package suite"
    implemented_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXRetirementUITests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXRetirementUITests test"
    wire_fixture: "RuntimeSafetyClient-owned recovery snapshot, runtime.recovery.required/action accessibility IDs, Recover Local Runtime action, and exact absence of Case File Review status/copy."
    expected_red_reason: "Observed RED at 6f7d57a0/5fedbf7a: removing the Review-only wrapper left no neutral cancellation-quarantine implementation or shipping owner-operated recovery surface."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-1.5/WP-3.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-XPC-CANCEL-01"
    finding_or_invariant: "Ordinary stream cancellation confirms exact ID/status/quiescence; mismatch, active-notFound, timeout, or transport error quarantines new data-plane work until owner-operated idle recovery."
    owner_work_package: "WP-3.1"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/RuntimeSafetyClientTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter RuntimeSafetyClientTests"
    wire_fixture: "Nondefault prompts 817 through 907; exact generation and mismatched IDs; cancelled/failed/notFound/other-active/timeout replies; three-attempt bounded policy; no data-plane call after quarantine."
    expected_red_reason: "Observed RED at 5fedbf7a: RuntimeSafetyClient lacked the injectable bounded cancellation-confirmation policy and exact mismatch/notFound/timeout behavior required by the matrix."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-RUNTIME-SCHED-01"
    finding_or_invariant: "Simultaneous production callers queue by foreground/user-batch/background/speculative policy with FIFO+aging, bounded N/N+1 backpressure, no raw busy leakage, and exactly one active GPU-backed operation."
    owner_work_package: "WP-3.1"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTRuntimeSched01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTRuntimeSched01Tests"
    wire_fixture: "Synthetic T_RUNTIME_SCHED_01_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at b5ba84a6: the shared coordinator, exact task/request/configuration types, finite priority queue, and one-lane admission owner were absent. GREEN at eca28d25/2f89d90d; the canonical selected gate now passes 2/2."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.1; RED at b5ba84a6; scheduler core at eca28d25; process-wide caller/composition routing at 2f89d90d."
    status: "green"
  - id: "T-RUNTIME-SCHED-02"
    finding_or_invariant: "A foreground request waits for background embedding to yield at a safe batch boundary and then takes the same physical GPU lane; obsolete prewarm is superseded, queued/active cancellation and successor quarantine are exact, and no dual-active interval occurs."
    owner_work_package: "WP-3.1"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTRuntimeSched02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTRuntimeSched02Tests"
    wire_fixture: "Synthetic T_RUNTIME_SCHED_02_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at b5ba84a6: no coordinator owned safe-boundary yield, prewarm supersession, or exact queued/active cancellation. Refinement RED at 38480234 pinned non-streaming cancellation ownership. GREEN at eca28d25/2f89d90d; the canonical selected gate now passes 5/5."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.1; RED at b5ba84a6; non-streaming cancellation refinement at 38480234; scheduler core at eca28d25; process-wide caller/composition routing at 2f89d90d."
    status: "green"
  - id: "T-NO-MULTI-AGENT-01"
    finding_or_invariant: "Release composition, public API, UI, Settings, diagnostics, persisted state, and dependency graph contain no agent-role registry, agent identity, agent handoff, autonomous recursion, orchestration framework, or concurrent generative worker. Fixed sequential model calls remain owned by one typed feature task and one shared GPU lane."
    owner_work_package: "WP-3.1"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTNoMultiAgent01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTNoMultiAgent01Tests"
    wire_fixture: "Synthetic T_NO_MULTI_AGENT_01_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at b5ba84a6: three source/composition gates found no shared coordinator contract and ordinary raw-runtime owners/calls outside the narrow transport allowlist. GREEN at eca28d25/2f89d90d; the canonical selected gate now passes 4/4."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.1; RED at b5ba84a6; shared coordinator core at eca28d25; ordinary caller and composition routing at 2f89d90d."
    status: "green"
  - id: "T-RUNTIME-LIFECYCLE-01"
    finding_or_invariant: "Every model-backed surface projects the same queued/preparing/running/cancelling/completed/failed/recoveryRequired states, duplicate suppression, relaunch behavior, progress/cancel action, and typed recovery."
    owner_work_package: "WP-3.1"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTRuntimeLifecycle01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTRuntimeLifecycle01Tests"
    wire_fixture: "Synthetic T_RUNTIME_LIFECYCLE_01_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at b5ba84a6: no shared lifecycle projection, duplicate suppression, relaunch policy, or typed recovery contract existed. GREEN at eca28d25/2f89d90d; the canonical selected gate now passes 2/2."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.1; RED at b5ba84a6; lifecycle owner at eca28d25; process-wide composition at 2f89d90d."
    status: "green"
  - id: "T-RUNTIME-BIND-01"
    finding_or_invariant: "Permit spans exact resolve/load/token-count/generate/terminal flush; another task cannot switch or substitute the content-bound model."
    owner_work_package: "WP-3.1"
    owner_phase: "Phase 3"
    target: "SupraSessions Swift package test target"
    implemented_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTRuntimeBind01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTRuntimeBind01Tests"
    wire_fixture: "Synthetic T_RUNTIME_BIND_01_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at b5ba84a6 and refinements 8f634f24/8cda5dff: no higher-level permit spanned exact resolve/load/token-count/generate/terminal flush, and chat/embedding loads could omit exact artifact binding. GREEN at e82650e6/ed2e9756/12d6960d; the canonical selected gate now passes 4/4."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.1; RED at b5ba84a6; binding refinements at 8f634f24 and 8cda5dff; permit implementation at e82650e6; exact chat/embedding profiles at ed2e9756 and 12d6960d."
    status: "green"
  - id: "T-XPC-BUDGET-01"
    finding_or_invariant: "Encoded bytes N/N+1 reject before decode."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTXpcBudget01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTXpcBudget01Tests"
    wire_fixture: "Synthetic T_XPC_BUDGET_01_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 21599179: directional encoded-byte budgets and reject-before-decode entry points were absent. GREEN at bacd83eb; the canonical selected gate now passes 3/3."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 21599179; bounded codec and host/client implementation at bacd83eb."
    status: "green"
  - id: "T-XPC-BUDGET-02"
    finding_or_invariant: "Batch/per-string/aggregate UTF-8 N/N+1."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTXpcBudget02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTXpcBudget02Tests"
    wire_fixture: "Synthetic T_XPC_BUDGET_02_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 21599179: batch, per-string, aggregate UTF-8, history-turn, and total-body semantic limits were absent. GREEN at bacd83eb; the canonical selected gate now passes 5/5."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 21599179; request semantic validation at bacd83eb."
    status: "green"
  - id: "T-XPC-BUDGET-03"
    finding_or_invariant: "Token and padded-element N/N+1, including one-long-row amplification."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTXpcBudget03Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTXpcBudget03Tests"
    wire_fixture: "Synthetic T_XPC_BUDGET_03_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 21599179: token-row, aggregate-token, and padded-element amplification limits were absent before MLX allocation. GREEN at bacd83eb; the canonical selected gate now passes 5/5."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 21599179; pre-allocation token-shape validation at bacd83eb."
    status: "green"
  - id: "T-XPC-BUDGET-04"
    finding_or_invariant: "Output event/byte overflow cannot create terminal partial state."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTXpcBudget04Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTXpcBudget04Tests"
    wire_fixture: "Synthetic T_XPC_BUDGET_04_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 21599179: replay event/encoded-byte/output-byte overflow could not be poisoned before terminal publication. GREEN at bacd83eb; the canonical selected gate now passes 4/4."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 21599179; bounded event accounting and terminal poisoning at bacd83eb."
    status: "green"
  - id: "T-XPC-BUDGET-05"
    finding_or_invariant: "Non-default model/hardware memory ceiling proves policy wiring."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTXpcBudget05Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTXpcBudget05Tests"
    wire_fixture: "Synthetic T_XPC_BUDGET_05_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 21599179: the host had no injectable resource policy or checked non-default model/hardware admission planner. Arithmetic refinement RED at 56c0e1ef pinned negative and overflow boundaries. GREEN at bacd83eb; the canonical selected gate now passes 2/2."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 21599179; fixture lookup correction at bdeac8ce; arithmetic RED at 56c0e1ef; checked resource admission at bacd83eb."
    status: "green"
  - id: "T-XPC-BUDGET-06"
    finding_or_invariant: "Token-count response and embedding vector count/dimension/scalar-byte N/N+1 limits are enforced."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTXpcBudget06Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTXpcBudget06Tests"
    wire_fixture: "Synthetic T_XPC_BUDGET_06_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 21599179: token-count and embedding response count/dimension/scalar-byte limits were absent. Arithmetic refinement RED at 56c0e1ef pinned pre-profile overflow rejection. GREEN at bacd83eb; the canonical selected gate now passes 5/5."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 21599179; arithmetic RED at 56c0e1ef; bounded response/profile validation at bacd83eb."
    status: "green"
  - id: "T-RUNTIME-KV-01"
    finding_or_invariant: "Weight/KV/activation/overhead/current-pressure model admits context N and rejects/repacks/defer N+1 without silent exact-evidence truncation or model substitution."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTRuntimeKv01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTRuntimeKv01Tests"
    wire_fixture: "Synthetic T_RUNTIME_KV_01_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 21599179: admission did not reserve output KV, honor the requested prompt ceiling, or distinguish visible ordinary-history trim from exact-evidence repack/defer. GREEN at bacd83eb; the canonical selected gate now passes 5/5."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 21599179; checked prompt/output-KV and workload-specific admission at bacd83eb."
    status: "green"
  - id: "T-RUNTIME-SWITCH-01"
    finding_or_invariant: "Low-headroom model replacement unloads old before new; safe-headroom swap remains transactional; no double-weight peak occurs outside policy."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTRuntimeSwitch01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTRuntimeSwitch01Tests"
    wire_fixture: "Synthetic T_RUNTIME_SWITCH_01_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 21599179: replacement planning did not distinguish unload-first low headroom from transactional safe-headroom swap. GREEN at bacd83eb; the canonical selected gate now passes 3/3."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 21599179; checked model-switch planner and host execution at bacd83eb."
    status: "green"
  - id: "T-STREAM-BUFFER-01"
    finding_or_invariant: "Replay/event/UI/persistence byte and event caps hold while final output remains exact across completion, cancellation, reconnect, and terminal flush."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTStreamBuffer01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTStreamBuffer01Tests"
    wire_fixture: "Synthetic T_STREAM_BUFFER_01_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 21599179: client accumulation and replay retention had no bounded coalescing/compaction contract preserving exact terminal output. GREEN at bacd83eb; the canonical selected gate now passes 3/3."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 21599179; bounded client stream buffer and host replay compaction at bacd83eb."
    status: "green"
  - id: "T-RUNTIME-RESIDENCY-01"
    finding_or_invariant: "Tight-profile startup has no unconditional dual-model residency; prewarm is headroom-gated and inactive embeddings evict first."
    owner_work_package: "WP-3.4"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTRuntimeResidency01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTRuntimeResidency01Tests"
    wire_fixture: "Synthetic T_RUNTIME_RESIDENCY_01_WIRE_731 prewarm; chat model model-wire-713 revision rev-7 at 250 bytes; embedding-wire-719/embed-rev-7 at 200 bytes; fixed residency 400; exact 700/850-byte headroom boundaries; default model/default priority absent."
    expected_red_reason: "Observed RED at 5d7cba5e: the runtime residency snapshot, policy, prewarm request/plan, pressure types, reset contract, and shipping ModelLibrary residency owner were absent, producing exact missing-symbol and source-wire failures."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.4; RED at 5d7cba5e; async assertion correction at 5be4cce2; exact XPC transport RED at f60826e8; process/XPC implementation at ce32c456."
    status: "green"
  - id: "T-RUNTIME-PRESSURE-01"
    finding_or_invariant: "Warning blocks speculative/background admissions; critical applies exact cancel/defer/purge/evict policy without corrupting active or committed work."
    owner_work_package: "WP-3.4"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTRuntimePressure01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTRuntimePressure01Tests"
    wire_fixture: "Synthetic T_RUNTIME_PRESSURE_01_WIRE_731 pressure snapshot; seven-action ceiling with N=7/N+1=8; background/speculative cancellation, 17-byte derived-cache purge, inactive embedding-before-reranker-before-chat eviction, and active work preservation; default model/default priority absent."
    expected_red_reason: "Observed RED at 5d7cba5e: no typed memory-pressure level, bounded action policy, queued-work classification, derived-cache purge owner, or shipping pressure-source composition existed."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.4; RED at 5d7cba5e; process scheduler/cache/XPC pressure implementation at ce32c456."
    status: "green"
  - id: "T-RUNTIME-RESET-01"
    finding_or_invariant: "Deterministic reset proves containers/buffers/caches unloaded and epoch advanced; connection invalidation alone cannot satisfy reset/restart."
    owner_work_package: "WP-3.4"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTRuntimeReset01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTRuntimeReset01Tests"
    wire_fixture: "Synthetic T_RUNTIME_RESET_01_WIRE_731 reset at epoch 7; exact chat/embedding identities, 17 cache bytes, three replay generations, seven buffered events, active-work refusal, exact retry, then epoch 8-to-9 request; DEFAULT-000 absent."
    expected_red_reason: "Observed RED at 5d7cba5e: no deterministic reset request/receipt, epoch authority, or cache/buffer/container clearing contract existed. Refinement RED at f8f9df1b proved shipping recovery restarted the connection but made zero residency-snapshot and zero reset calls."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.4; RED at 5d7cba5e; transport RED at f60826e8; deterministic recovery RED at f8f9df1b; bounded process/XPC reset implementation at ce32c456."
    status: "green"
  - id: "T-XPC-METHOD-01"
    finding_or_invariant: "Every remaining protocol method is classified and enforced as ordinary data plane, recovery/control plane, or independently admitted."
    owner_work_package: "WP-3.2"
    owner_phase: "Phase 3"
    target: "SupraRuntimeClient Swift package test target"
    implemented_test_path: "Packages/SupraRuntimeClient/Tests/SupraRuntimeClientTests/ArchitectureUXTXpcMethod01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraRuntimeClient --filter ArchitectureUXTXpcMethod01Tests"
    wire_fixture: "Synthetic T_XPC_METHOD_01_WIRE_731 task; model model-wire-713 revision rev-7; queue N 7 and N+1 8; foreground priority; default model/default priority absent."
    expected_red_reason: "Observed RED at 4cefb00d: the exhaustive method catalog, typed classification policy, feature/recovery protocol split, finite classification receipt, and explicit transport routes were absent. GREEN at b86a50f1; the canonical selected gate now passes 5/5."
    forbidden_default_or_side_effect: "No dual-active GPU work, raw busy leakage, unbounded queue/buffer, model substitution, silent truncation, uncontrolled prewarm, or false terminal state."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS hosted app/XPC evidence, including current and peak app, XPC, and combined resource observations where applicable."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-3.2; RED at 4cefb00d; binding-gate protocol correction at 0f379640; method-plane implementation at b86a50f1."
    status: "green"
  - id: "T-PARITY-CHAT-01"
    finding_or_invariant: "Extracted use case preserves source order/digest, citations, assurance, cancellation, and Store effects."
    owner_work_package: "WP-4.1"
    owner_phase: "Phase 4"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTParityChat01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTParityChat01Tests"
    wire_fixture: "Synthetic T_PARITY_CHAT_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Observed RED at 7c348b97: GroundedChatTerminalPublicationRequest and GroundedChatTerminalPublicationUseCase were absent; the remaining diagnostics cascaded from those missing result types. Test-author refinement at f54501c2 removed a redundant second use-case execution after Store completion while retaining the durable digest assertion. Local GREEN at 67b568ff: the selected parity gate passes 2/2 and the existing atomic controller publication gates pass 4/4. The full SupraSessions run executed 1,127 tests and retained 56 unrelated assertions in 10 known legacy court-identity/runtime-composition cases, so protected selected execution and the fixed fourteen-package matrix remain pending."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-4.1; RED at 7c348b97; test-author refinement at f54501c2; extracted use-case implementation at 67b568ff."
    status: "red"
  - id: "T-PARITY-ENV-01"
    finding_or_invariant: "Extracted composition/fixtures/probes preserve production and signed Release behavior."
    owner_work_package: "WP-4.2"
    owner_phase: "Phase 4"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTParityEnv01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTParityEnv01Tests test"
    wire_fixture: "Synthetic T_PARITY_ENV_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTParityEnv01Tests is pending and the T-PARITY-ENV-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-4.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-ARCH-EDGE-01"
    finding_or_invariant: "Allowed dependency graph, exceptions, cycles, package inventory, and forbidden capability edges."
    owner_work_package: "WP-4.5"
    owner_phase: "Phase 4"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXTArchEdge01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXTArchEdge01Tests"
    wire_fixture: "Synthetic T_ARCH_EDGE_01_WIRE_731 record; identity record-713; policy version 8; reviewed exception N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Observed RED at f87be83d: ArchitecturePackageGraph, ArchitecturePackageEdge, and RepositoryArchitecturePolicy were absent; the remaining diagnostics cascaded from those missing owner types. Initial local GREEN at ac952ad8 pinned the version-seven 37-edge graph. Store-free Networking refinement RED at 340bfe1b then failed on the old policy version, required Networking to Store edge, and missing forbidden-capability classification. Version-eight GREEN at bec9afe6: the selected architecture gate passes 5/5 on the exact 36-edge graph, full SupraTestKit passes 58 tests with one explicit credential-gated live skip, and the executable package inventory verifier confirms all 14 packages. Protected selected execution and the fixed fourteen-package matrix at the exact candidate SHA remain pending."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-4.5; original RED at f87be83d; version-seven policy at ac952ad8; Store-free graph RED at 340bfe1b; version-eight policy at bec9afe6."
    status: "red"
  - id: "T-STORE-CAP-01"
    finding_or_invariant: "Narrow capability cannot perform unrelated writes; privileged backup/restore and benchmark paths retain parity."
    owner_work_package: "WP-4.3"
    owner_phase: "Phase 4"
    target: "SupraStore Swift package test target"
    pending_test_path: "Packages/SupraStore/Tests/SupraStoreTests/ArchitectureUXTStoreCap01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraStore --filter ArchitectureUXTStoreCap01Tests"
    wire_fixture: "Synthetic T_STORE_CAP_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Observed RED at 48cc8bf6: NetworkRequestAuditEntry, NetworkRequestAuditWriting, NetworkRequestAuditCapabilityError, and SupraStore.networkRequestAudits were absent; the remaining diagnostics cascaded from those missing owner types. Fixture refinement at 8a610dca removed an invalid unseeded research-session foreign key without weakening the exact write boundary. Local GREEN at 7fd5d6cb: the selected capability gate passes 3/3, backup/restore compatibility passes 28/28, full SupraStore passes 368/368, and full SupraCore passes 99/99. Protected selected execution and the fixed fourteen-package matrix at the exact candidate SHA remain pending."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-4.3; RED at 48cc8bf6; fixture correction at 8a610dca; narrow Store capability at 7fd5d6cb."
    status: "red"
  - id: "T-UI-PROJECTION-01"
    finding_or_invariant: "Non-default record-to-view mappings preserve identity, update/delete, sorting, and filtering."
    owner_work_package: "WP-4.4"
    owner_phase: "Phase 4"
    target: "SupraSessions Swift package test target"
    pending_test_path: "Packages/SupraSessions/Tests/SupraSessionsTests/ArchitectureUXTUiProjection01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraSessions --filter ArchitectureUXTUiProjection01Tests"
    wire_fixture: "Synthetic T_UI_PROJECTION_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Observed RED at df553439: MatterDocumentSummary, DocumentFolderSummary, BillingLineView, ScratchPadSearchHit, DiagnosticsController, and DiagnosticEventSummary were absent; the remaining diagnostics cascaded from those missing projection owners. Local GREEN at 1ba925fd: selected T-UI-PROJECTION-01 passes 5/5, affected existing document/billing/ScratchPad controller suites pass 39/39, and the unsigned Debug app plus embedded XPC build succeeds without launching the native UI-test runner. Full SupraSessions executes 1,132 tests; its remaining 56 assertions (10 unexpected) are confined to inherited drafting and motion fixtures that still supply legacy free-text court identity and stop at the canonical Choose Court gate, outside WP-4.4. Protected selected execution and the fixed fourteen-package matrix at the exact candidate SHA remain pending."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-4.4; RED at df553439; UI projection GREEN at 1ba925fd."
    status: "red"
  - id: "T-NET-AUDIT-01"
    finding_or_invariant: "Pre-send failure means zero transport; selected post-response behavior, redaction/fingerprints, content-free payload, and no Networking→Store edge are exact."
    owner_work_package: "WP-4.6"
    owner_phase: "Phase 4"
    target: "SupraNetworking Swift package test target"
    pending_test_path: "Packages/SupraNetworking/Tests/SupraNetworkingTests/ArchitectureUXTNetAudit01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraNetworking --filter ArchitectureUXTNetAudit01Tests"
    wire_fixture: "Synthetic T_NET_AUDIT_01_WIRE_731 matter; provider provider-713; exact query QUERY-CANARY-719; grant version 7; unrelated matter body DEFAULT-BODY-000 must not transmit."
    expected_red_reason: "Observed RED at c373c918: NetworkRequestLogger had no Core-writer initializer and remained coupled to NetworkRequestRepository; the remaining diagnostics cascaded from that missing boundary. Test-author corrections at 8540334b, 938cd482, and 8ce24790 made the throwing transport, deterministic identity closure, and exact header casing executable without weakening the assertions. Local GREEN at bec9afe6: selected T-NET-AUDIT-01 passes 3/3, full SupraNetworking passes 43/43, full SupraStore passes 369/369, affected Sessions controller suites pass 30/30, full SupraTestKit passes 58 tests with one explicit credential-gated live skip, the unsigned Debug app build succeeds, and repository facts confirm the 14-package inventory. Protected selected execution and the fixed fourteen-package matrix at the exact candidate SHA remain pending."
    forbidden_default_or_side_effect: "No transport, model call, Store mutation, replay, implicit matter fallback, or terminal success outside the exact grant/packet contract."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-4.3 narrow capability at 7fd5d6cb; WP-4.6 RED at c373c918; fixture corrections at 8540334b, 938cd482, and 8ce24790; Store-free Networking GREEN at bec9afe6."
    status: "red"
  - id: "T-OOXML-01"
    finding_or_invariant: "Normalized XML, styles, relationships, and package parts are byte/semantic equivalent."
    owner_work_package: "WP-6.1"
    owner_phase: "Phase 6"
    target: "SupraDocuments Swift package test target"
    pending_test_path: "Packages/SupraDocuments/Tests/SupraDocumentsTests/ArchitectureUXTOoxml01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraDocuments --filter ArchitectureUXTOoxml01Tests"
    wire_fixture: "Synthetic T_OOXML_01_WIRE_731 artifact; intent artifact-713; digest digest-719; collision suffix 7; default overwrite path DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTOoxml01Tests is pending and the T-OOXML-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-6.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-UTILITY-01"
    finding_or_invariant: "Versioned canonical JSON/digest vectors are frozen and byte exact."
    owner_work_package: "WP-6.2"
    owner_phase: "Phase 6"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXTUtility01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXTUtility01Tests"
    wire_fixture: "Synthetic T_UTILITY_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTUtility01Tests is pending and the T-UTILITY-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-6.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-MARKDOWN-01"
    finding_or_invariant: "Any shared renderer preserves parsing, streaming, citation taps, tables, accessibility, and visual parity."
    owner_work_package: "WP-6.2"
    owner_phase: "Phase 6"
    target: "SupraDesignSystem Swift package test target"
    pending_test_path: "Packages/SupraDesignSystem/Tests/SupraDesignSystemTests/ArchitectureUXTMarkdown01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraDesignSystem --filter ArchitectureUXTMarkdown01Tests"
    wire_fixture: "Synthetic T_MARKDOWN_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTMarkdown01Tests is pending and the T-MARKDOWN-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-6.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-MIGRATION-01"
    finding_or_invariant: "Exact v001-current order, shipping fixtures, schema, indexes, triggers, integrity, and foreign keys."
    owner_work_package: "WP-6.3"
    owner_phase: "Phase 6"
    target: "SupraStore Swift package test target"
    pending_test_path: "Packages/SupraStore/Tests/SupraStoreTests/ArchitectureUXTMigration01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraStore --filter ArchitectureUXTMigration01Tests"
    wire_fixture: "Synthetic T_MIGRATION_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTMigration01Tests is pending and the T-MIGRATION-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No swallowed error, partial aggregate, overwrite, input loss, success-only dismissal/navigation, or committed-state drift."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-6.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-REVIEW-RETIRE-CAP-01"
    finding_or_invariant: "Exact scans/compile policy find zero public Case File Review views, repositories, controllers, composition, export, queue, runtime, Settings, or status capability."
    owner_work_package: "WP-4.7"
    owner_phase: "Phase 4"
    target: "Repository retirement-verifier self-test"
    implemented_test_path: "Tests/Scripts/test-case-file-review-retirement-verifier.sh"
    selected_test_command: "bash Tests/Scripts/test-case-file-review-retirement-verifier.sh"
    wire_fixture: "Synthetic shipping tree with one CaseFileReviewBackdoor, stale migrator and deletion-shim uses, duplicated guided-review identity, exact frozen v073 allowlists, and nine retained concepts."
    expected_red_reason: "Observed RED at 78b2c633: the first real-tree scan reported 694 retired findings across 21 exact categories before product removal."
    forbidden_default_or_side_effect: "No live Case File Review capability, admission, runtime reservation, mutation, export, claim, or UI route; immutable compatibility history and unrelated review semantics remain."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-4.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-REVIEW-RETIRE-SCHEMA-01"
    finding_or_invariant: "Byte-identical `v072` and shared-main `v073` remain ordered/recognized; v069/v072/v073 fixtures reach the actual final endpoint; Review schema is dormant/unreachable; restore/reopen/integrity/downgrade posture match."
    owner_work_package: "WP-1.5"
    owner_phase: "Phase 1"
    target: "SupraStore Swift package test target"
    implemented_test_path: "Packages/SupraStore/Tests/SupraStoreTests/ArchitectureUXReviewRetirementSchemaTests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraStore --filter ArchitectureUXReviewRetirementSchemaTests"
    wire_fixture: "Hash-pinned v072/v073/reset closures; raw-SQL dormant project with nondefault matter/project IDs; seven-table schema/index/FK contract; permanent source deletion with live evidence edge; repeated file-backed reopen."
    expected_red_reason: "Observed RED at bdeeb24e: removing the public Review repository exposed that immutable v073's evidence trigger blocked ordinary permanent source deletion until a private hash-pinned compatibility degradation ran atomically."
    forbidden_default_or_side_effect: "No live Case File Review capability, admission, runtime reservation, mutation, export, claim, or UI route; immutable compatibility history and unrelated review semantics remain."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-1.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-REVIEW-RETIRE-CLAIMS-01"
    finding_or_invariant: "All Case File Review claims/docs/help are gone; unrelated research-review, selected corpus, runtime, hardware, and export claims remain accurate."
    owner_work_package: "WP-1.5/Phase 7 qualification"
    owner_phase: "Phases 1 and 7"
    target: "Repository retirement-verifier and verified-claims meta-tests"
    implemented_test_path: "Tests/Scripts/test-case-file-review-retirement-verifier.sh"
    selected_test_command: "bash Tests/Scripts/test-case-file-review-retirement-verifier.sh && bash Scripts/verify-product-claims.sh && bash Tests/Scripts/test-verify-product-claims.sh"
    wire_fixture: "Exact STORE-MIGRATION-SEQUENCE v078 shipping endpoint plus a duplicate stale v073 Review claim fixture; 43 current claims; retained attorney-review and package-only corpus wording."
    expected_red_reason: "Observed RED at 78b2c633: eight live Case File Review claims and current architecture/help wording still advertised the feature; a duplicate v073 endpoint outside the migration claim also bypassed a path-wide scan."
    forbidden_default_or_side_effect: "No live Case File Review capability, admission, runtime reservation, mutation, export, claim, or UI route; immutable compatibility history and unrelated review semantics remain."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-1.5/Phase 7 qualification; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "T-REVIEW-RETIRE-SMOKE-01"
    finding_or_invariant: "Smoke/CI manifests contain no stale TRPUI/TRPCREATEUI/TRPHWUI selectors and match the exact nonzero executed test set."
    owner_work_package: "WP-1.5/Phase 7 qualification"
    owner_phase: "Phases 1 and 7"
    target: "Repository macOS CI manifest meta-test"
    implemented_test_path: "Tests/Scripts/test-macos-ci-gates.sh"
    selected_test_command: "bash Tests/Scripts/test-macos-ci-gates.sh"
    wire_fixture: "Synthetic stale TRPUI/TRPCREATEUI/TRPHWUI selectors, commented or missing five-method ArchitectureUXRetirementUITests manifest, zero-execution and exact-selector mismatch fixtures."
    expected_red_reason: "Observed RED at 4124a59b: the smoke and CI manifests still named deleted Review selectors and could silently reduce native coverage."
    forbidden_default_or_side_effect: "No live Case File Review capability, admission, runtime reservation, mutation, export, claim, or UI route; immutable compatibility history and unrelated review semantics remain."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-1.5/Phase 7 qualification; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "DR-CORPUS-OWNER-01"
    finding_or_invariant: "Post-removal audit proves Review-v2 queue composition absent and names generic corpus consumers; fewer than two means no extraction, while any broader retirement requires a separate owner decision."
    owner_work_package: "WP-6.6"
    owner_phase: "Phase 6"
    target: "Repository retirement-verifier self-test and architecture decision receipt"
    implemented_test_path: "Tests/Scripts/test-case-file-review-retirement-verifier.sh"
    selected_test_command: "bash Tests/Scripts/test-case-file-review-retirement-verifier.sh"
    wire_fixture: "Synthetic AppEnvironment composes CorpusAnalysisQueueRunner.live through corpusAnalysisRunner while using no retired Review token; exact package-only runner and ExhaustiveListTask substrate remain required."
    expected_red_reason: "Observed RED at 2b9ac8dc: the retirement verifier accepted a shipping AppEnvironment that silently composed the retained generic runner, so the recorded zero-consumer/no-extraction decision was not enforced."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-6.6; predecessor RED evidence and exact-head baseline where applicable."
    status: "green"
  - id: "DR-PACKAGE-01"
    finding_or_invariant: "Decision record freezes package baseline, quantitative continue/stop threshold, and pilot outcome; no change occurs from disk size alone."
    owner_work_package: "WP-6.4"
    owner_phase: "Phase 6"
    target: "SupraTestKit Swift package test target"
    pending_test_path: "Packages/SupraTestKit/Tests/SupraTestKitTests/ArchitectureUXDrPackage01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer swift test --package-path Packages/SupraTestKit --filter ArchitectureUXDrPackage01Tests"
    wire_fixture: "Synthetic DR_PACKAGE_01_WIRE_731 record; identity record-713; version 7; N 7 and N+1 8; default identity DEFAULT-000 forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXDrPackage01Tests is pending and the DR-PACKAGE-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default fixture value, silent skip, tautology, partial side effect, implicit fallback, or success state unsupported by authoritative postconditions."
    required_native_evidence: "Selected deterministic package result; add a native journey only when this invariant reaches a user-facing surface."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Versioned synthetic fixture or installed artifact report with exact identity, digest, and native inspection where applicable."
    dependencies: "WP-0.4 canonical ledger; WP-6.4; predecessor RED evidence and exact-head baseline where applicable."
    status: "decision_pending"
  - id: "T-IA-SIDEBAR-01"
    finding_or_invariant: "Exact Work/Utilities membership/order plus curated System Status; internal IDs/paths/probes absent ordinarily and available only in Advanced."
    owner_work_package: "WP-5.1"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTIaSidebar01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTIaSidebar01Tests test"
    wire_fixture: "Synthetic T_IA_SIDEBAR_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTIaSidebar01Tests is pending and the T-IA-SIDEBAR-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-IA-MATTER-01"
    finding_or_invariant: "Exact order Chat/Documents/Research/Authorities/Saved Work/Billing/Activity, explicit Review absence, compact More geometry/reachability at 880."
    owner_work_package: "WP-5.1"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTIaMatter01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTIaMatter01Tests test"
    wire_fixture: "Synthetic T_IA_MATTER_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTIaMatter01Tests is pending and the T-IA-MATTER-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.1; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-CHAT-UX-01"
    finding_or_invariant: "New placeholder/starters and optional slash shortcut; starters cannot bypass setup, authority, routing, egress, or zero-side-effect blocks."
    owner_work_package: "WP-5.2"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTChatUx01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTChatUx01Tests test"
    wire_fixture: "Synthetic T_CHAT_UX_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTChatUx01Tests is pending and the T-CHAT-UX-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-CHAT-UX-02"
    finding_or_invariant: "Task-governed default—and any evidence-approved Focused/Standard/Exploratory control—uses non-default wire proofs; ordinary copy never implies sampling establishes legal accuracy."
    owner_work_package: "WP-5.2"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTChatUx02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTChatUx02Tests test"
    wire_fixture: "Synthetic T_CHAT_UX_02_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTChatUx02Tests is pending and the T-CHAT-UX-02 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-CHAT-UX-03"
    finding_or_invariant: "At 880/1100, popovers stay contained and whole citation rows meet contrast/size and pointer/keyboard activation gates."
    owner_work_package: "WP-5.2"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTChatUx03Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTChatUx03Tests test"
    wire_fixture: "Synthetic T_CHAT_UX_03_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTChatUx03Tests is pending and the T-CHAT-UX-03 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.2; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-SETTINGS-01"
    finding_or_invariant: "Category order, state/deep links, Advanced isolation, and zero Review-specific role/advisory/setup/status/path/control."
    owner_work_package: "WP-5.3"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTSettings01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTSettings01Tests test"
    wire_fixture: "Synthetic T_SETTINGS_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTSettings01Tests is pending and the T-SETTINGS-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.3; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-TRUST-PATTERN-01"
    finding_or_invariant: "Remaining source-backed surfaces preserve independently required exact-source/model/scope/evidence receipts without Case File Review production code."
    owner_work_package: "WP-5.4"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTTrustPattern01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTTrustPattern01Tests test"
    wire_fixture: "Synthetic T_TRUST_PATTERN_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTTrustPattern01Tests is pending and the T-TRUST-PATTERN-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.4; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-DRAFT-UX-01"
    finding_or_invariant: "Unavailable types are not interactive; ordinary warning states selected-source, review, applicability, and filing-readiness limits."
    owner_work_package: "WP-5.5"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTDraftUx01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTDraftUx01Tests test"
    wire_fixture: "Synthetic T_DRAFT_UX_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTDraftUx01Tests is pending and the T-DRAFT-UX-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-SAVED-WORK-01"
    finding_or_invariant: "Rows show type/issue/source status/author-reviewer/last-reviewed date; route/model hidden ordinarily; record/source identity unchanged."
    owner_work_package: "WP-5.5"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTSavedWork01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTSavedWork01Tests test"
    wire_fixture: "Synthetic T_SAVED_WORK_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTSavedWork01Tests is pending and the T-SAVED-WORK-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-SAVED-WORK-02"
    finding_or_invariant: "Membership and reviewed semantics are exact; every version binds matter/source/authority/task/model lineage; identity/source/model/authority changes mark old work stale/needs-check without silent retargeting through export."
    owner_work_package: "WP-5.5"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTSavedWork02Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTSavedWork02Tests test"
    wire_fixture: "Synthetic T_SAVED_WORK_02_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTSavedWork02Tests is pending and the T-SAVED-WORK-02 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-RELATION-01"
    finding_or_invariant: "Exact direction and audit persist; downstream stays blocked until Confirm/Reject/Override, and override reason is durable."
    owner_work_package: "WP-5.5"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTRelation01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTRelation01Tests test"
    wire_fixture: "Synthetic T_RELATION_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTRelation01Tests is pending and the T-RELATION-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-ERROR-01"
    finding_or_invariant: "Fixture-specific save-new-copy/retry text is ordinary; raw code/JSON absent until Technical Details and present after expansion."
    owner_work_package: "WP-5.6"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTError01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTError01Tests test"
    wire_fixture: "Synthetic T_ERROR_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTError01Tests is pending and the T-ERROR-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.6; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-RECOVERY-01"
    finding_or_invariant: "Show Recovery Folder, Copy Diagnostic Report, and Quit Without Changes are visible; collapsed details alone contain database/blob facts."
    owner_work_package: "WP-5.6"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTRecovery01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTRecovery01Tests test"
    wire_fixture: "Synthetic T_RECOVERY_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTRecovery01Tests is pending and the T-RECOVERY-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.6; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-COMPONENT-01"
    finding_or_invariant: "Headers and audited sheets/panels satisfy title/purpose/primary/overflow, Esc/default action, focus, width, selection, and trailing-inspector contracts."
    owner_work_package: "WP-5.7"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTComponent01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTComponent01Tests test"
    wire_fixture: "Synthetic T_COMPONENT_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTComponent01Tests is pending and the T-COMPONENT-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-TRUST-CONTRACT-01"
    finding_or_invariant: "Every trust state maps to one comprehensible ordinary label/icon/action/next step and exact detail disclosure at decision point, inspector, and export preflight; essential evidence is not Advanced-only."
    owner_work_package: "WP-5.7"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTTrustContract01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTTrustContract01Tests test"
    wire_fixture: "Synthetic T_TRUST_CONTRACT_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTTrustContract01Tests is pending and the T-TRUST-CONTRACT-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-A11Y-01"
    finding_or_invariant: "Essential actions exist before hover and are keyboard/VoiceOver reachable."
    owner_work_package: "WP-5.7"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTA11y01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTA11y01Tests test"
    wire_fixture: "Synthetic T_A11Y_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTA11y01Tests is pending and the T-A11Y-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-CONTRAST-01"
    finding_or_invariant: "System Status, source metadata, billing confidence, and citations meet normal and Increase Contrast text tokens."
    owner_work_package: "WP-5.7"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTContrast01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTContrast01Tests test"
    wire_fixture: "Synthetic T_CONTRAST_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTContrast01Tests is pending and the T-CONTRAST-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-TERMS-01"
    finding_or_invariant: "Ordinary rendered/accessibility text uses the approved attorney mapping; internal terms appear only in authorized disclosures."
    owner_work_package: "WP-5.7"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTTerms01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTTerms01Tests test"
    wire_fixture: "Synthetic T_TERMS_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTTerms01Tests is pending and the T-TERMS-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.7; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
  - id: "T-PUBLIC-RECORDS-01"
    finding_or_invariant: "First-use acronyms and synthetic examples retain source limitations and trigger zero idle-state network calls."
    owner_work_package: "WP-5.5"
    owner_phase: "Phase 5"
    target: "SupraAIUITests hosted native-app target"
    pending_test_path: "Apps/SupraAI/SupraAIUITests/ArchitectureUXTPublicRecords01Tests.swift"
    selected_test_command: "DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer xcodebuild -workspace SupraAI.xcworkspace -scheme SupraAI -destination 'platform=macOS' -only-testing:SupraAIUITests/ArchitectureUXTPublicRecords01Tests test"
    wire_fixture: "Synthetic T_PUBLIC_RECORDS_01_WIRE_731 workspace; matter matter-713; intent draftMotion; source-set source-719 v7; authority packet packet-727 v11; return checkpoint checkpoint-733; implicit current-matter/default route forbidden."
    expected_red_reason: "Expected RED: ArchitectureUXTPublicRecords01Tests is pending and the T-PUBLIC-RECORDS-01 wire fixture is not yet enforced end to end; first failure must be a missing symbol or exact invariant mismatch, never a skip."
    forbidden_default_or_side_effect: "No default/current-matter fallback, global broadcast, second main window, context loss, dead-end blocker, hidden essential action, or false success navigation."
    required_native_evidence: "Native macOS journey evidence at 880, 1100, and wide widths, including keyboard and accessibility assertions."
    required_hosted_evidence: "Protected macOS selected test plus the fixed fourteen-package matrix at the exact candidate SHA."
    required_artifact_evidence: "Executed-test manifest and exact candidate SHA; no separate installed artifact is required for this contract."
    dependencies: "WP-0.4 canonical ledger; WP-5.5; predecessor RED evidence and exact-head baseline where applicable."
    status: "red"
